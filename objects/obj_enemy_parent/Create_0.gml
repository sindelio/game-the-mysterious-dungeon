// Variables to follow player
target_x = x
target_y = y

// Battle variables
hp_total = hp

// Start the routine to check for the player distance
alarm[0] = 60

// Enemies must collide with walls too
collision_tilemap = layer_tilemap_get_id("Tiles_Collision")